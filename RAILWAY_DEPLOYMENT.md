# 🚂 Развертывание на Railway за 5 минут

Пошаговая инструкция как развернуть backend на Railway.

## 📋 Что нужно

- Аккаунт GitHub (твой код должен быть на GitHub)
- Аккаунт на Railway (регистрация бесплатная)
- Этот код в папке `/1.DEV/2.DEV_docs/youtube-transcript-api`

## ✅ Вариант 1: Самый простой (через GitHub)

### Шаг 1: Загрузить код на GitHub

```bash
# Если еще нет репозитория
git init
git add .
git commit -m "Add YouTube Transcript API backend"
git remote add origin https://github.com/твой-логин/chrome-extension-repo.git
git push -u origin main
```

### Шаг 2: Открыть Railway

1. Перейди на https://railway.app
2. Нажми "Login with GitHub" (или создай аккаунт)
3. Авторизуй Railway доступ к твоему GitHub

### Шаг 3: Создать новый проект

1. Нажми "New Project"
2. Выбери "Deploy from GitHub"
3. Выбери твой репозиторий
4. Нажми "Deploy"

### Шаг 4: Указать папку

1. Railway спросит "Which repo would you like to deploy?"
2. Выбери ветку (обычно `main`)
3. В настройках проекта:
   - Нажми на сервис
   - В "Environment" или "Settings" укажи ROOT_DIR или подобное
   - Или просто перенеси этот код в корень репозитория

### Шаг 5: Получить URL

После развертывания Railway выдаст URL типа:
```
https://youtube-subtitles-api-production.up.railway.app
```

**Готово!** 🎉

---

## 🔧 Вариант 2: Через Railway CLI

### Шаг 1: Установить Railway CLI

```bash
# macOS
brew install railway

# Windows
choco install railway

# Или скачай отсюда: https://docs.railway.app/guides/cli
```

### Шаг 2: Авторизироваться

```bash
railway login
```

Откроется браузер, авторизуйся через GitHub.

### Шаг 3: Инициализировать проект

```bash
cd /path/to/youtube-transcript-api
railway init
```

Railway спросит имя проекта. Введи что-то типа:
```
youtube-subtitles-api
```

### Шаг 4: Развернуть

```bash
railway up
```

Будет показан прогресс развертывания. Когда закончится, ты получишь URL.

### Шаг 5: Просмотреть логи

```bash
railway logs
```

---

## 🔍 Проверить развертывание

После развертывания проверь что все работает:

```bash
# Проверить здоровье (замени на свой URL)
curl https://youtube-subtitles-api-production.up.railway.app/api/health

# Должен вернуть:
# {"ok": true, "service": "YouTube Subtitles API", "timestamp": "..."}
```

## 📊 Railway Dashboard

После развертывания можешь:
- Просмотреть логи сервера
- Перезагрузить приложение
- Масштабировать (добавить больше CPU/RAM)
- Управлять переменными окружения
- Просмотреть статистику использования

Все это в Railway Dashboard: https://railway.app/dashboard

## 🐛 Troubleshooting

### "Build failed" или "Deployment failed"

1. Проверь что `requirements.txt` существует
2. Проверь что `Procfile` содержит: `web: python app.py`
3. Посмотри в логах Railway что произошло

### "Module not found" в логах

```
ModuleNotFoundError: No module named 'flask'
```

Это значит что зависимости не установились. Проверь:
- `requirements.txt` находится в корне папки (которую развертываешь)
- В файле указаны все нужные пакеты

### "Connection refused" или "Service unavailable"

Может быть:
1. Сервис еще загружается (подожди 30 секунд)
2. В коде `app.py` ошибка (посмотри логи)
3. Railway сервис не запустился (попробуй перезагрузить)

### Port error

Если видишь ошибку про порт, убедись что в `app.py`:

```python
PORT = int(os.getenv('PORT', 5000))
app.run(host='0.0.0.0', port=PORT)
```

Railway автоматически установит переменную `PORT`.

## 💰 Стоимость

**Хорошие новости:**
- Railway дает **$5 free credits в месяц**
- Этот сервер будет стоить примерно **$1-3 в месяц**
- Значит ты получишь **бесплатно**! 🎉

Тарифы:
- CPU: $0.07 в час
- Memory (RAM): $0.014 за GB в час

## 📈 После развертывания

1. **Обнови URL в расширении:**
   - Открой `js/backend/transcript-api-client.js`
   - Замени `API_URL` на свой URL из Railway

2. **Протестируй:**
   ```javascript
   await YouTubeTranscriptApiClient.getSubtitles('E19_kwN0f38', 'en');
   ```

3. **Смотри логи:**
   ```bash
   railway logs
   ```

4. **Включи CI/CD** (опционально):
   - Railway автоматически развертывает при каждом push в GitHub
   - Можешь удалять текущий сервис и создавать новый при каждом обновлении
   - Или настроить автоматическое обновление

## 🔄 Обновление кода

Если изменишь код и хочешь обновить сервер:

### Вариант 1: Через GitHub (автоматически)

```bash
# Просто push в GitHub
git add .
git commit -m "Fix subtitle extraction"
git push origin main

# Railway автоматически перезагрузит сервер!
```

### Вариант 2: Через Railway CLI (вручную)

```bash
cd youtube-transcript-api
railway up
```

## 📚 Дополнительно

- Railway docs: https://docs.railway.app
- Python on Railway: https://docs.railway.app/guides/python
- Troubleshooting: https://docs.railway.app/guides/troubleshooting

## ✨ Готово!

После развертывания:
1. ✅ Backend работает на Railway
2. ✅ URL можно использовать в расширении
3. ✅ Автоматическое масштабирование
4. ✅ Бесплатно первый месяц (благодаря $5 credits)

**Следующий шаг:** интегрируй URL в расширение и тестируй! 🚀
