# YouTube Subtitle API - Инициализация проекта

## 📌 О проекте

Серверная часть расширения Chrome для извлечения субтитров из видео YouTube. Сервер будет развернут на **Railway** и предоставляет REST API для работы с субтитрами.

### Основная функция
- Получение субтитров из YouTube видео
- Поддержка различных языков
- Опциональный перевод субтитров
- Обработка ошибок (видео без субтитров, недоступные видео и т.д.)

---

## 🛠️ Технологический стек

- **Язык**: Python 3.x
- **Фреймворк**: Flask 3.0.0
- **Основная библиотека**: youtube-transcript-api 0.6.1
- **Дополнительно**:
  - python-dotenv для управления переменными окружения
  - flask-cors для поддержки CORS (расширение Chrome)
  - requests для HTTP запросов

---

## 📂 Структура проекта

```
.
├── app.py                    # Основной Flask сервер
├── requirements.txt          # Зависимости Python
├── Procfile                  # Конфиг для Railway
├── .env.example              # Пример переменных окружения
├── README.md                 # Основная документация
├── RAILWAY_DEPLOYMENT.md     # Инструкция по развертыванию
├── INTEGRATION_GUIDE.md      # Руководство интеграции с расширением
└── .claude/                  # Конфигурация Claude Code
    └── init.md              # Этот файл
```

---

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Подготовка окружения
```bash
cp .env.example .env
```

### 3. Запуск сервера локально
```bash
python app.py
```

Сервер запустится на `http://localhost:5000`

---

## 📡 API Эндпоинты

### POST `/api/subtitles`

Получить субтитры из YouTube видео

**Request body:**
```json
{
  "videoId": "E19_kwN0f38",
  "language": "en",
  "translateTo": null
}
```

**Параметры:**
- `videoId` (string) - ID видео YouTube
- `language` (string) - Язык оригинальных субтитров (ISO код, например "en", "ru")
- `translateTo` (string|null) - Язык перевода (если требуется), или null для отключения перевода

**Response (200 OK):**
```json
{
  "videoId": "E19_kwN0f38",
  "language": "en",
  "subtitles": [
    {
      "text": "Hello world",
      "start": 0,
      "duration": 2
    }
  ],
  "success": true
}
```

**Error Response (400/500):**
```json
{
  "error": "Описание ошибки",
  "success": false
}
```

---

## ⚙️ Развертывание на Railway

1. Зайти в Railway и создать новый проект
2. Подключить Git репозиторий
3. Railway автоматически прочитает Procfile и развернет приложение
4. Установить PORT переменную окружения (обычно 5000)

Подробнее см. `RAILWAY_DEPLOYMENT.md`

---

## 🔧 Основные команды для работы

```bash
# Запуск сервера
python app.py

# Установка новой зависимости
pip install <package_name>

# Обновление requirements.txt
pip freeze > requirements.txt

# Тестирование API локально
curl -X POST http://localhost:5000/api/subtitles \
  -H "Content-Type: application/json" \
  -d '{"videoId": "dQw4w9WgXcQ", "language": "en"}'
```

---

## 📚 Документация

- **RAILWAY_DEPLOYMENT.md** - Полный гайд по развертыванию на Railway
- **INTEGRATION_GUIDE.md** - Как интегрировать API в Chrome расширение
- **README.md** - Общая информация о проекте

---

## ✅ Хорошие практики при разработке

1. **Логирование** - Проверяйте логи для отладки проблем
2. **Переменные окружения** - Используйте .env для конфиденциальных данных
3. **Обработка ошибок** - API правильно обрабатывает ошибки YouTube
4. **CORS** - Расширение Chrome требует правильную настройку CORS
5. **Производительность** - Кешируйте результаты если возможно

---

## 🐛 Решение типичных проблем

| Проблема | Решение |
|----------|---------|
| `TranscriptsDisabled` | Видео не имеет доступных субтитров |
| `NoTranscriptFound` | Запрашиваемый язык не доступен |
| `VideoUnavailable` | Видео удалено или недоступно |
| CORS ошибки | Проверить настройки CORS в app.py |

---

## 📝 Примечания для разработчиков

- Основной файл приложения: `app.py`
- Точка входа: `/api/subtitles`
- По умолчанию работает на порту 5000 (можно изменить через PORT переменную)
- Flask работает в debug режиме, если DEBUG=true в .env
