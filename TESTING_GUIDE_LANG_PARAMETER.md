# 🧪 Тестирование параметра LANG - Инструкция

## 📌 Что было добавлено?

**Тестовая версия GET endpoint** с поддержкой параметра `lang` для безопасного тестирования получения автоматически сгенерированных YouTube субтитров на разных языках.

---

## 🚀 Быстрый старт

### Основной тестовый endpoint:

```
GET /api/subtitles/test/<videoId>?lang=<language>
```

### Примеры:

```bash
# Получить русские субтитры (если доступны)
curl 'https://web-production-bd8bb.up.railway.app/api/subtitles/test/DvKZlIiiQGM?lang=ru'

# Получить немецкие субтитры
curl 'https://web-production-bd8bb.up.railway.app/api/subtitles/test/DvKZlIiiQGM?lang=de'

# Получить французские субтитры
curl 'https://web-production-bd8bb.up.railway.app/api/subtitles/test/DvKZlIiiQGM?lang=fr'

# Оригинальный язык (без параметра lang)
curl 'https://web-production-bd8bb.up.railway.app/api/subtitles/test/DvKZlIiiQGM'
```

---

## 📊 Endpoint мониторинга

### Отслеживание нагрузки на YouTube API:

```bash
curl 'https://web-production-bd8bb.up.railway.app/api/monitoring'
```

### Что показывает:

```json
{
  "success": true,
  "service": "YouTube Subtitles API Monitoring",
  "monitoring_data": {
    "requests_per_minute": 6,      // ← Запросы в минуту
    "requests_per_hour": 6,         // ← Запросы в час
    "status": "healthy",            // ← Статус (healthy/warning/critical)
    "health_alerts": {
      "warning_at": 10,             // ← Предупреждение при 10+ запросах/мин
      "critical_at": 100            // ← Критично при 100+ запросов/час
    },
    "recent_requests": [            // ← Последние 10 запросов
      {
        "video_id": "DvKZlIiiQGM",
        "endpoint": "GET_TEST",
        "language": "ru",
        "status": "success",
        "time_ago": "6s ago"
      }
    ]
  }
}
```

---

## 🧪 Тестовые сценарии

### Сценарий 1: Проверить доступные языки

```bash
#!/bin/bash

VIDEO_ID="DvKZlIiiQGM"
LANGUAGES=("en" "ru" "de" "fr" "es" "ja" "pt" "it")

echo "Проверка доступных языков для видео: $VIDEO_ID"
echo "================================================"

for lang in "${LANGUAGES[@]}"; do
  echo "Тест на $lang..."
  curl -s "https://web-production-bd8bb.up.railway.app/api/subtitles/test/$VIDEO_ID?lang=$lang" | \
    jq "{lang: .requested_language, result: .language, count: .count, success: .success}"
done

echo ""
echo "Статистика мониторинга:"
curl -s "https://web-production-bd8bb.up.railway.app/api/monitoring" | \
  jq '.monitoring_data | {requests_per_minute, requests_per_hour, status}'
```

### Сценарий 2: Нагрузочное тестирование

```bash
#!/bin/bash

echo "Нагрузочное тестирование (10 одновременных запросов)"
echo "===================================================="

VIDEO_ID="DvKZlIiiQGM"

# 10 параллельных запросов
for i in {1..10}; do
  (curl -s "https://web-production-bd8bb.up.railway.app/api/subtitles/test/$VIDEO_ID?lang=ru" > /dev/null && \
    echo "✅ Запрос $i успешен") &
done

wait

echo ""
echo "Проверка статуса после нагрузки:"
curl -s "https://web-production-bd8bb.up.railway.app/api/monitoring" | \
  jq '.monitoring_data | {requests_per_minute, requests_per_hour, status}'
```

---

## 📈 Результаты тестирования (2025-11-20)

### Тест 1: Базовая функциональность ✅

```
Запрос: GET /api/subtitles/test/DvKZlIiiQGM?lang=ru
Результат:
{
  "success": true,
  "language": "en",              // ← Оригинальный язык
  "requested_language": "ru",    // ← Что мы запросили
  "count": 763
}

Интерпретация: YouTube не имеет auto-generated на русском
              для этого видео, поэтому вернул оригинальный (English)
```

### Тест 2: Мониторинг работает ✅

```
Запросов в минуту: 6 (из допустимых 60)
Запросов в час: 6 (из допустимых 360+)
Статус: healthy ✅

Вывод: Нет риска блокировки YouTube
```

### Тест 3: Rate limiting работает ✅

```
Между каждым запросом: 0.5 сек задержка (по дизайну)
Это предотвращает блокировку при параллельных запросах
```

---

## 🔍 Интерпретация результатов

### Если `requested_language` ≠ `language`:

```javascript
{
  "requested_language": "ru",     // ← Мы просили русский
  "language": "en"                // ← Но вернули английский
}
```

**Это означает:**
- YouTube не имеет auto-generated субтитров на русском для этого видео
- API автоматически вернул оригинальный язык (fallback)
- **Это нормально и безопасно!**

### Если `requested_language` === `language`:

```javascript
{
  "requested_language": "de",     // ← Мы просили немецкий
  "language": "de"                // ← И получили немецкий
}
```

**Это означает:**
- YouTube имеет auto-generated субтитры на немецком
- API вернул точно то что вы просили ✅

---

## ⚠️ Мониторинг статуса

### Status: HEALTHY ✅

```
requests_per_minute: 0-10
requests_per_hour: 0-100
```

**Действие:** Продолжайте тестирование, все нормально.

### Status: WARNING ⚠️

```
requests_per_minute: 10+
```

**Действие:**
- Уменьшить частоту тестов
- Проверить в логах что случилось
- Дождаться сброса счетчика (через 1 минуту)

### Status: CRITICAL 🔴

```
requests_per_hour: 100+
```

**Действие:**
- **НЕМЕДЛЕННО ОСТАНОВИТЬ тестирование!**
- YouTube может заблокировать IP
- Дождаться сброса счетчика (через 1 час)
- Сообщить в support

---

## 🛡️ Защита от блокировки

### Встроенные защиты:

1. ✅ **Rate Limiter**
   - Максимум 2 запроса к YouTube в секунду
   - Автоматически задерживает запросы если нужно

2. ✅ **Request Monitor**
   - Отслеживает все запросы
   - Автоматически выявляет проблемы
   - Предупреждает при превышении лимитов

3. ✅ **Fallback механизм**
   - Если запрошенный язык не найден
   - Автоматически вернет оригинальный язык
   - Не приводит к ошибке

---

## 💡 Рекомендации по тестированию

### ✅ Делайте:

- Тестируйте разные видео
- Тестируйте разные языки
- Проверяйте мониторинг между тестами
- Используйте небольшие интервалы между запросами
- Дождитесь успешного ответа перед следующим тестом

### ❌ НЕ делайте:

- Не отправляйте 100+ запросов подряд
- Не игнорируйте предупреждения (status: warning)
- Не пытайтесь обойти rate limiter
- Не тестируйте в боевом сервере (только в тестовом /test)

---

## 📝 Команды для тестирования

### Быстрый тест одного видео:

```bash
VIDEO_ID="DvKZlIiiQGM"
echo "Тест оригинального языка:"
curl -s "https://web-production-bd8bb.up.railway.app/api/subtitles/test/$VIDEO_ID" | jq '.language, .count'

echo "Тест русского:"
curl -s "https://web-production-bd8bb.up.railway.app/api/subtitles/test/$VIDEO_ID?lang=ru" | jq '.language, .count'
```

### Полный отчет с мониторингом:

```bash
echo "=== Тестирование ===" && \
curl -s "https://web-production-bd8bb.up.railway.app/api/subtitles/test/DvKZlIiiQGM?lang=ru" | \
  jq '{success, language, requested_language, count}' && \
echo "" && \
echo "=== Мониторинг ===" && \
curl -s "https://web-production-bd8bb.up.railway.app/api/monitoring" | \
  jq '.monitoring_data | {requests_per_minute, requests_per_hour, status}'
```

---

## 🎯 Далее - после тестирования

### Если тестирование успешно ✅

Проведя несколько дней тестирования и убедившись что:
1. API возвращает правильные результаты
2. YouTube не блокирует нас
3. Мониторинг показывает здоровое состояние

**Решение:** Мигрировать функциональность в основной GET endpoint.

### Если есть проблемы ❌

Если видны:
1. Частые ошибки
2. Статус WARNING или CRITICAL
3. YouTube блокирует запросы

**Решение:** Вернуться к базовому GET endpoint (без lang поддержки).

---

## 📞 Поддержка

Вопросы или проблемы? Проверьте:
1. Статус мониторинга: `GET /api/monitoring`
2. Валидность video ID (11 символов)
3. Мониторинг логов в Railway console
4. Документацию в README

---

## 📚 Связанные документы

- [WHY_NO_TRANSLATION_IN_GET.md](./WHY_NO_TRANSLATION_IN_GET.md) - Объяснение архитектуры
- [EXTENSION_2_INTEGRATION_GUIDE.md](./EXTENSION_2_INTEGRATION_GUIDE.md) - Основная документация
- [GET_ENDPOINT_DOCS.md](./GET_ENDPOINT_DOCS.md) - Техническая документация

---

**Статус:** 🧪 Test version
**Версия:** 2.1-beta
**Дата:** 2025-11-20
**Сервер:** web-production-bd8bb.up.railway.app
