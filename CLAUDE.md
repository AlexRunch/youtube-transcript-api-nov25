# CLAUDE.md

Этот файл содержит рекомендации для Claude Code (claude.ai/code) при работе с кодом в этом репозитории.

## Обзор проекта

**YouTube Subtitles API Backend** — Flask сервер REST API, который извлекает субтитры из YouTube для расширения Chrome. Сервер развернут на Railway и выступает посредником между расширением Chrome и YouTube, предоставляя возможность извлечения субтитров с поддержкой нескольких языков и опциональным переводом.

**Архитектура:**
```
Chrome Extension → REST API (этот сервер) → YouTube
```

## Технологический стек

- **Язык:** Python 3.x
- **Фреймворк:** Flask 3.0.0
- **Основная библиотека:** youtube-transcript-api 0.6.1
- **Дополнительно:** python-dotenv, flask-cors, requests, Werkzeug 3.0.0
- **Развертывание:** Railway
- **Конфигурация сервера:** Procfile

## Архитектура приложения

### Основные компоненты

1. **app.py** (~327 строк)
   - Единый файл Flask приложения со всей логикой API
   - Три основных API эндпоинта: `/api/health`, `/api/subtitles`, `/api/languages/<video_id>`
   - Обработчики ошибок для типичных исключений YouTube API
   - CORS поддержка для запросов из расширения Chrome
   - Детальное логирование с временными метками и emoji индикаторами

2. **Структура API**
   - **GET /api/health** — Проверка здоровья сервера
   - **POST /api/subtitles** — Главный эндпоинт для получения субтитров
     - Входные данные: `videoId` (строка), `language` (строка), `translateTo` (опционально)
     - Выходные данные: Массив объектов субтитров с полями `time`, `duration`, `text`
   - **GET /api/languages/<video_id>** — Список доступных языков субтитров для видео

3. **Ключевые функции**
   - `format_subtitles()` — Преобразует формат youtube-transcript-api во внутренний формат (меняет `start` → `time`)
   - `get_available_languages()` — Получает список вручную созданных и автоматически сгенерированных субтитров
   - Обработка ошибок: TranscriptsDisabled, NoTranscriptFound, VideoUnavailable

### Поток данных

1. Расширение Chrome отправляет POST запрос с ID видео
2. Сервер валидирует ID видео (ровно 11 символов)
3. Сервер запрашивает YouTube на наличие доступных транскриптов
4. Если запрашиваемый язык недоступен, переходит на первый доступный язык
5. Опциональный перевод применяется если указан параметр `translateTo`
6. Субтитры форматируются и возвращаются в JSON

## Типичные команды разработки

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск сервера локально (разработка)
python app.py

# Тестирование API эндпоинта
curl -X POST http://localhost:5000/api/subtitles \
  -H "Content-Type: application/json" \
  -d '{"videoId": "E19_kwN0f38", "language": "en"}'

# Проверка здоровья
curl http://localhost:5000/api/health

# Обновление зависимостей
pip freeze > requirements.txt
```

## Важные детали реализации

### Валидация Video ID
- Должен быть ровно 11 символов
- Возвращает 400 Bad Request если неверный формат
- Пустой videoId возвращает 400 с сообщением об ошибке

### Обработка языков
- Если запрашиваемый язык не найден, сервер автоматически использует первый доступный язык
- Поддерживает как вручную созданные, так и автоматически сгенерированные субтитры
- Возвращает список доступных языков в ответе для информирования клиента

### Перевод
- Перевод опционален (параметр `translateTo` может быть null)
- Если перевод не удается, сервер корректно возвращается к исходному языку
- Перевод осуществляется через метод `.translate()` youtube-transcript-api

### Обработка ошибок
- Специфичные HTTP коды: 400 (неверный запрос), 403 (субтитры отключены), 404 (недоступно), 500 (ошибка сервера)
- Все ошибки возвращают JSON с `{"success": false, "error": "message"}`
- Детальное логирование с временными метками для отладки

### Конфигурация CORS
- Настроена для origins Chrome расширения: `chrome-extension://*`
- Также разрешает YouTube: `https://*.youtube.com`
- Корректно работает если flask-cors не установлен

### Конфигурация развертывания
- **Порт:** Читается из переменной окружения PORT (по умолчанию 5000)
- **Host:** Должен слушать на `0.0.0.0` для совместимости с Railway
- **Debug режим:** Контролируется переменной окружения DEBUG
- **Procfile:** Содержит `web: python app.py` для Railway

## Точки интеграции

### Интеграция с Chrome расширением
- Клиентский модуль ожидает глобальный объект `YouTubeTranscriptApiClient`
- Должна быть реализована логика повтора (3 попытки с задержкой 1000ms)
- Таймаут запроса: 30 секунд
- URL должен быть обновлен в коде клиента после развертывания на Railway

### Развертывание на Railway
- Автоматически определяет Python проект по requirements.txt
- Автоматически устанавливает переменную окружения PORT
- Поддерживает автоматическое развертывание с GitHub при push
- Бесплатный уровень: $5 ежемесячных кредитов (достаточно для ~$1-3 месячного использования)

## Структура файлов

```
.
├── app.py                    # Основное Flask приложение
├── requirements.txt          # Python зависимости
├── Procfile                  # Конфиг развертывания Railway
├── .env.example              # Шаблон переменных окружения
├── README.md                 # Пользовательская документация
├── RAILWAY_DEPLOYMENT.md     # Подробный гайд развертывания
├── INTEGRATION_GUIDE.md      # Гайд интеграции с расширением Chrome
├── .claude/
│   └── init.md              # Гайд инициализации проекта
└── CLAUDE.md                # Этот файл
```

## Формат ответов

### Успешный ответ с субтитрами
```json
{
  "success": true,
  "videoId": "E19_kwN0f38",
  "language": "en",
  "translatedTo": null,
  "subtitles": [
    {"time": 0.5, "duration": 1.5, "text": "Hello"},
    {"time": 2.3, "duration": 2.1, "text": "World"}
  ],
  "count": 2,
  "availableLanguages": [...]
}
```

### Ответ со списком языков
```json
{
  "success": true,
  "videoId": "E19_kwN0f38",
  "languages": [
    {"code": "en", "name": "English", "isAuto": false},
    {"code": "es", "name": "Spanish", "isAuto": true}
  ]
}
```

## Ключевые моменты

1. **Преобразование формата субтитров** — youtube-transcript-api использует поле `start`, но API возвращает `time`. Это намеренно и обрабатывается в `format_subtitles()`.

2. **Стратегия логирования** — Использует emoji индикаторы (✅, ❌, ⚠️, 📥, 🌐) для быстрого сканирования логов. Не меняйте их.

3. **Fallback по языкам** — Когда запрашиваемый язык недоступен, сервер выбирает первый доступный (приоритет вручную созданным). Это предотвращает ошибки API.

4. **Производительность** — Типичное время ответа: 200-400ms для извлечения субтитров.

5. **Надежность** — YouTube API иногда дает сбой. Клиент должен реализовать логику повтора (уже сделано в модуле клиента расширения Chrome).

6. **Безопасность** — Валидация Video ID предотвращает инъекции. Не требуется аутентификация пользователя (stateless API).

## Решение типичных проблем

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `TranscriptsDisabled` | Видео без субтитров | Это нормально, вернуть соответствующую ошибку |
| `NoTranscriptFound` | Язык недоступен | Fallback на первый доступный (уже реализовано) |
| `VideoUnavailable` | Видео удалено/приватное | Вернуть ошибку 404 |
| CORS ошибки | flask-cors не установлен | Уже обработано с try/except |
| Ошибка привязки порта | PORT уже используется | Изменить переменную окружения PORT |

## Перед внесением изменений

- Все поля запроса/ответа используются клиентом расширения Chrome
- Валидация Video ID (11 символов) строгая в целях безопасности
- Преобразование формата субтитров (`start` → `time`) требуется для совместимости
- Формат логирования с emoji помогает при отладке в production на Railway
- CORS заголовки необходимы для общения расширения Chrome с сервером
