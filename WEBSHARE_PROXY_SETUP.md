# Настройка Webshare Static Residential Proxy для Railway

## Проблема

Railway использует IP адреса облачных провайдеров, которые часто блокируются YouTube. Это приводит к ошибкам типа:

```
YouTube is blocking requests from your IP. This usually is due to one of the following reasons:
- You have done too many requests and your IP has been blocked by YouTube
- You are doing requests from an IP belonging to a cloud provider (like AWS, Google Cloud Platform, Azure, etc.)
```

## Решение

Использование **Static Residential прокси** от Webshare позволяет:
- Избежать блокировки Railway IP
- Использовать реальные residential IP адреса
- Обеспечить стабильную работу API

## Шаг 1: Получение данных прокси из Webshare

1. Зайдите в свой аккаунт Webshare: https://proxy.webshare.io/
2. Перейдите в раздел **Static Residential** → **Proxy List**
3. Выберите любой активный прокси из списка (статус "Working")
4. Скопируйте данные:
   - **Proxy Address**: например `63.141.62.166:6459`
   - **Username**: например `hhlnixdt`
   - **Password**: например `54tssmyl37of`

## Шаг 2: Настройка переменных окружения в Railway

1. Откройте ваш проект в Railway: https://railway.app/
2. Перейдите в раздел **Settings** → **Variables**
3. Добавьте следующие переменные окружения:

### Обязательные переменные для прокси:

```bash
# Адрес прокси сервера (IP:порт)
WEBSHARE_PROXY_ADDRESS=63.141.62.166:6459

# Логин для аутентификации на прокси
WEBSHARE_PROXY_USERNAME=hhlnixdt

# Пароль для аутентификации на прокси
WEBSHARE_PROXY_PASSWORD=54tssmyl37of
```

### Пример заполнения в Railway:

| Variable Name | Value |
|--------------|-------|
| `WEBSHARE_PROXY_ADDRESS` | `63.141.62.166:6459` |
| `WEBSHARE_PROXY_USERNAME` | `hhlnixdt` |
| `WEBSHARE_PROXY_PASSWORD` | `54tssmyl37of` |

**⚠️ ВАЖНО:** Замените значения на свои реальные данные из Webshare!

## Шаг 3: Проверка настройки

После добавления переменных окружения Railway автоматически перезапустит приложение.

### Проверка через логи Railway:

В логах вы должны увидеть:
```
✅ Прокси настроен: hhlnixdt@63.141.62.166:6459
🔒 YouTube запросы будут идти через Webshare Static Residential прокси
```

Если прокси НЕ настроен, вы увидите:
```
⚠️ Прокси не настроен - используется прямое подключение к YouTube
⚠️ Установите переменные окружения: WEBSHARE_PROXY_ADDRESS, WEBSHARE_PROXY_USERNAME, WEBSHARE_PROXY_PASSWORD
⚠️ Без прокси Railway IP может быть заблокирован YouTube
```

### Проверка через API:

```bash
# Проверка здоровья сервера
curl https://your-railway-app.railway.app/api/health

# Проверка детального статуса (показывает прокси статус)
curl https://your-railway-app.railway.app/api/status
```

### Тестовый запрос субтитров:

```bash
curl -X POST https://your-railway-app.railway.app/api/subtitles \
  -H "Content-Type: application/json" \
  -d '{
    "videoId": "dQw4w9WgXcQ",
    "language": "en"
  }'
```

Если всё настроено правильно, запрос должен успешно вернуть субтитры без ошибок блокировки.

## Шаг 4: Ротация прокси (опционально)

Webshare предоставляет 20 прокси адресов. Если один прокси блокируется YouTube:

1. Перейдите в Webshare → **Static Residential** → **Proxy List**
2. Найдите прокси со статусом "Working" (зеленая галочка)
3. Нажмите **Replace Proxy** для проблемного прокси
4. Обновите переменную `WEBSHARE_PROXY_ADDRESS` в Railway с новым адресом
5. Railway автоматически перезапустит приложение с новым прокси

## Отключение прокси (для тестирования)

Если нужно временно отключить прокси:

1. В Railway → Settings → Variables
2. Удалите все три переменные: `WEBSHARE_PROXY_ADDRESS`, `WEBSHARE_PROXY_USERNAME`, `WEBSHARE_PROXY_PASSWORD`
3. Или установите их в пустые значения

Приложение вернется к прямому подключению к YouTube (может быть заблокировано).

## Мониторинг работы прокси

### Endpoint `/api/status`

```bash
curl https://your-railway-app.railway.app/api/status | jq
```

Возвращает:
- `status`: healthy / warning / critical / blocked
- `risk_score`: 0-100 (риск блокировки)
- `youtube_metrics`: статистика запросов и ошибок
- `daily_stats`: статистика за день
- `recommendation`: рекомендации по действиям

### Telegram алерты (опционально)

Для получения уведомлений о блокировках настройте:

```bash
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
ENABLE_TELEGRAM_ALERTS=true
```

## Стоимость и лимиты

- **Webshare Static Residential**: ~$2.99/GB
- **Средний расход**: ~1-2 MB на 100 запросов субтитров
- **Рекомендуемый план**: 1 GB = достаточно для ~50,000-100,000 запросов

## Устранение проблем

### Прокси не работает (429/403 ошибки продолжаются)

1. Проверьте правильность данных (Address, Username, Password)
2. Убедитесь что прокси имеет статус "Working" в Webshare
3. Попробуйте другой прокси из списка (Replace Proxy)
4. Проверьте баланс в Webshare (может закончиться трафик)

### Медленные запросы через прокси

Static Residential прокси могут добавлять 100-300ms к каждому запросу. Это нормально.

Типичное время ответа:
- Без прокси: 200-400ms
- С прокси: 400-700ms

### Прокси заблокирован YouTube

Замените прокси на другой из списка Webshare (см. "Ротация прокси" выше).

## Дополнительная информация

- **Webshare Dashboard**: https://proxy.webshare.io/
- **Railway Dashboard**: https://railway.app/
- **API документация**: См. README.md в проекте

---

**✅ После настройки всех переменных ваш API будет работать через прокси и избегать блокировок YouTube!**
